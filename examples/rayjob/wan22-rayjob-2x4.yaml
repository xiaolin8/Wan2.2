apiVersion: ray.io/v1
kind: RayJob
metadata:
  name: wan22-rayjob-2x4
spec:
  entrypoint: python generate_ray.py --task t2v-A14B --size "832*480" --ckpt_dir /models/Wan2.2-T2V-A14B
    --ulysses_size 8 --prompt "A cinematic video of an astronaut walking on Mars and discovering a giant stone engraved with 'TenxCloud'."
    --save_file /workspace/outputs/$(date '+%Y%m%d%H%M%S')-tenxcloud-from-ray.mp4 --num-workers 8
    --sample_guide_scale 10 --convert_model_dtype --offload_model True --dit_fsdp
  shutdownAfterJobFinishes: false
  clusterSelector:
    ray.io/cluster: wan22-ray-cluster
